git init=> to initialize git on project


 git add -A to add files to staged state(now git tracking all files)


git commit
 git commit -m it gives message

git config --global user.email "s.r.barbate@gmail.com"
git config --global user.name "Sonal"
 git branch